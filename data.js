/* ============================================================
   Deployed data — what every visitor sees.

   The admin panel writes to this browser's localStorage only.
   To publish your edits to everyone:
     Admin → Reset → "Saytga chop etish (data.js)"
     → replace this file with the downloaded one → deploy.
   ============================================================ */
// window.DEPLOYED_DATA = { ... };
